{
    "name": "aurin-esp",
    "version": "0.0.1",
    "engines": {
      "node": "0.10.29"
    },
    "scripts": {
        "postinstall": "cd programs/server && rm -rf node_modules && npm install && npm install bcrypt@0.7.7 && mkdir -p npm/npm-bcrypt/node_modules && mv node_modules/bcrypt npm/npm-bcrypt/node_modules && cd ../.."
    }
}
